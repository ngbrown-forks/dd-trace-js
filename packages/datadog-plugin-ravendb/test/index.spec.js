'use strict'

const { ERROR_MESSAGE, ERROR_STACK, ERROR_TYPE } = require('../../dd-trace/src/constants')
const agent = require('../../dd-trace/test/plugins/agent')

describe('Plugin', () => {
    let elasticsearch
    let tracer

    withVersions('ravendb', ['ravendb'], (version, moduleName) => {
    })
})
